function add(a,b){
    console.log(a+b);
}
add(10,15);
function subtract(a,b){
    return a-b
}
subtract(100,50)
//if you run now, you won't see the output so must use console.log outside.
// output is there but is saved in memory to see it, you must .log outside
//mainly when writing company info dont coonsole
console.log(subtract(100,50));
//keyword arg. and pa
